use std::path::PathBuf;

use super::Error;

#[allow(dead_code)]
pub fn create_genesis_file() -> Result<PathBuf, Error> {
    let path = PathBuf::new();
    Ok(path)
}

#[allow(dead_code)]
pub fn create_spec_file() -> Result<PathBuf, Error> {
    let path = PathBuf::new();
    Ok(path)
}
